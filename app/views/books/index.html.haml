- @countries.each do |country|
  %article.book_country
    %header
      %h3= link_to "#{country.name}"
    .books.row
      - country.recent_books.each do |book|
        .book.col-sm-2
          =link_to image_tag(book.small_cover_url), book