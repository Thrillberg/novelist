- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div.has-form{:class => "alert-box #{name.to_s == 'notice' ? 'success' : 'alert'}", "data-alert" => ""}
      = content_tag :div, msg
      %a.close{:href => "#"} ×